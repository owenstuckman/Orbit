# Orbit — TODO

Remaining work. For completed features see `docs/FEATURES.md`. For tasks requiring human action see `docs/HUMAN_TODO.md`.

---

## Human Steps Remaining

All remaining tasks require manual action outside the codebase. See `docs/HUMAN_TODO.md`:

- iOS native project setup (requires macOS + Xcode)
- Firebase Cloud Messaging setup (push notifications, requires Firebase Console)
- Email flow end-to-end verification (pending DNS propagation)
